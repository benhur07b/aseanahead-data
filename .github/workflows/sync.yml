name: Sync sheet data

on:
  schedule:
    # Every 15 minutes, off the :00/:15/:30/:45 marks—GitHub delays or drops
    # schedules that fire on the popular slots.
    - cron: '2,17,32,47 * * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch and validate sheet data
        run: python3 sync.py

      # Runs even when a sheet failed (partial success still publishes the
      # valid file); the job stays red so the failure is visible.
      - name: Commit updated data
        if: ${{ !cancelled() }}
        run: |
          if git diff --quiet progress.csv reach.csv events.csv meta.json; then
            echo "No data changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add progress.csv reach.csv events.csv meta.json
          git commit -m "data: sync from Google Sheets"
          git push
